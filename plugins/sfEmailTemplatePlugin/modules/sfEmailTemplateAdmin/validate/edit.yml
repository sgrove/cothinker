fields:
  sf_email_template{name}:
    required:
      msg: You must provide name
    sfStringValidator:
      max:          50
      max_error:    Too many characters
  sf_email_template{subject}:
    required:
      msg: You must provide subject
    sfStringValidator:
      max:          255
      max_error:    Too many characters
  sf_email_template{body}:
    required:
      msg: You must provide body

fillin:
  activate: true
