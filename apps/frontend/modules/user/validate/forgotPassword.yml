fillin:
  enabled: true

fields:
  username:
    required:
      msg:       *You must enter a valid email address
    sfEmailValidator:
      strict:       true
      email_error:  This email address is invalid
    userExistsValidator:
      user_error: Sorry, couldn't find this person in our records - probably means no one signed up using this name.
